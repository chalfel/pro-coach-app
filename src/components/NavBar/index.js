import React from 'react';
import { Container, HomeIcon, AccountIcon } from './styles'

const NavBar = () => {
  return (
    <Container>
      <HomeIcon />
      <AccountIcon />
    </Container>
  );
}

export default NavBar;